name: handover

services:
  handover:
    build: ./app
    container_name: handover
    restart: unless-stopped
    ports:
      - "8100:8100"
    environment:
      HANDOVER_ROOT: /data
      HANDOVER_READONLY: "false"
    volumes:
      - ./data:/data
    user: "1000:1000"
